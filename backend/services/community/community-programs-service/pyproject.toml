[tool.poetry]
name = "community-programs-service"
version = "0.1.0"
description = "Community programs catalog microservice"
authors = ["Rommel Pachacama"]

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.0"
pymongo = "^4.6.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
