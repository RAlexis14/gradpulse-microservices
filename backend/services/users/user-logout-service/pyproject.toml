[tool.poetry]
name = "user-logout-service"
version = "0.1.0"
description = "User logout microservice with Redis blacklist"
authors = ["Rommel Pachacama"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.0"
redis = "^5.0.0"
pyjwt = "^2.8.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
